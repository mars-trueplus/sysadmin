<odoo>
  <data>
    <record id="go_inside_change_list_product" model="ir.ui.view">
      <field name="name">abc he he</field>
      <field name="model">product.template</field>
      <field name="inherit_id" ref="product.product_template_tree_view"/>
      <field name="arch" type="xml">
        <xpath expr="//field[@name='sequence']" position="before">
          <field name='responsible_id'/>
        </xpath>
      </field>
    </record>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="go_inside.list">
      <field name="name">go_inside list</field>
      <field name="model">go_inside.go_inside</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="go_inside.action_window">
      <field name="name">go_inside window</field>
      <field name="res_model">go_inside.go_inside</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="go_inside.action_server">
      <field name="name">go_inside server</field>
      <field name="model_id" ref="model_go_inside_go_inside"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="go_inside" id="go_inside.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="go_inside.menu_1" parent="go_inside.menu_root"/>
    <menuitem name="Menu 2" id="go_inside.menu_2" parent="go_inside.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="go_inside.menu_1_list" parent="go_inside.menu_1"
              action="go_inside.action_window"/>
    <menuitem name="Server to list" id="go_inside" parent="go_inside.menu_2"
              action="go_inside.action_server"/>
    -->
  </data>
</odoo>